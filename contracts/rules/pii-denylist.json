{
  "_doc": "Property, parameter or header names that can never appear in the contract (constitution VII: OPE stores no identifying information). Case-insensitive comparison. Extending this list is the only way to add a prohibition; the ope-no-pii rule reads it from here. The exception for `password` in the portal login will be implemented as a Spectral override scoped to that schema when it exists.",
  "deny": [
    "email",
    "name",
    "firstName",
    "lastName",
    "phone",
    "address",
    "document",
    "dni",
    "ip",
    "ipAddress",
    "card",
    "cardNumber",
    "password"
  ]
}
