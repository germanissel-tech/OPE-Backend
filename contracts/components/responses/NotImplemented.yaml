description: The operation exists in the contract but the server registered no handler for it.
content:
  application/problem+json:
    schema:
      $ref: ../schemas/ProblemDetails.yaml
    example:
      type: urn:ope:problem:not-implemented
      title: Operation declared without a handler
      status: 501
      instance: /v1/health
