type: string
description: Punto de anclaje semántico donde se renderiza (o se renderizó) una intervención. El SDK lo resuelve con el mapa de anclajes del merchant.
enum: [size_selector, price, cta, policies]
