# Fixture of tests/contract-diff (FR-020): brk-remove-operation. Compared against base.yaml.
openapi: 3.1.0
info:
  title: Fixture diff
  version: 1.0.0
paths:
  /v1/health:
    get:
      operationId: getHealth
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                type: object
                required:
                  - status
                properties:
                  status:
                    type: string
                  version:
                    type: string
        "500":
          description: err
