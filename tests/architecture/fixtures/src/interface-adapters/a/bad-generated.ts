// Violation (generated-only-from-http-core): a module of the ring reading the generated contract types directly instead of through the core.
import type { paths } from "#generated/api.js";
export type V = paths;
