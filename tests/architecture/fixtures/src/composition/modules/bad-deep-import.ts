// Violation (composition-imports-module-index): the composition importing a file of the ring that is not the index of its module.
import { ok } from "../../interface-adapters/ledger/controllers/ok.js";
export const badDeep = { ok };
