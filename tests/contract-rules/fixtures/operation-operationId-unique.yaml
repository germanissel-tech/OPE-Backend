# Fixture de tests/contract-rules: operation-operationId-unique. Generado a partir de un contrato mínimo válido; viola sólo la regla que lleva en el nombre.
openapi: 3.1.0
info:
  title: Fixture
  version: 1.0.0
  description: Contrato mínimo de prueba.
  contact:
    name: OPE
servers:
  - url: /
tags:
  - name: system
    description: Sistema.
paths:
  /v1/health:
    get:
      operationId: getHealth
      tags:
        - system
      summary: Salud del fixture
      description: Devuelve el estado.
      security: []
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Health"
              example:
                status: ok
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/things:
    post:
      operationId: getHealth
      tags:
        - system
      summary: Crea una cosa
      description: Crea una cosa.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: Cosa a crear.
              additionalProperties: false
              required:
                - kind
              properties:
                kind:
                  type: string
                  description: Clase de cosa.
                  enum:
                    - a
                    - b
                meta:
                  type: object
                  description: Metadatos.
                  additionalProperties: false
                  properties:
                    note:
                      type: string
                      description: Nota.
            example:
              kind: a
              meta:
                note: n
      responses:
        "201":
          description: Creada.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Health"
              example:
                status: ok
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"
components:
  schemas:
    Health:
      type: object
      description: Estado.
      additionalProperties: false
      required:
        - status
      properties:
        status:
          type: string
          description: Salud.
    ProblemDetails:
      type: object
      description: Problem Details (RFC 9457).
      additionalProperties: false
      required:
        - type
        - title
        - status
      properties:
        type:
          type: string
          description: Tipo de problema.
        title:
          type: string
          description: Título.
        status:
          type: integer
          description: Código HTTP.
  responses:
    InternalServerError:
      description: Error interno.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          example:
            type: urn:ope:problem:x
            title: t
            status: 500
    BadRequest:
      description: Request inválido.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          example:
            type: urn:ope:problem:x
            title: t
            status: 400
    UnprocessableEntity:
      description: No procesable.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          example:
            type: urn:ope:problem:x
            title: t
            status: 422
