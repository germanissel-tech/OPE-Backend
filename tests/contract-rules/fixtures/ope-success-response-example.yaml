# Fixture of tests/contract-rules: ope-success-response-example. Generated by gen-fixtures.mjs from a minimal valid contract; violates only the rule in its name.
openapi: 3.1.0
info:
  title: Fixture
  version: 1.0.0
  description: Minimal test contract.
  contact:
    name: OPE
servers:
  - url: /
tags:
  - name: system
    description: Sistema.
paths:
  /v1/health:
    get:
      operationId: getHealth
      tags:
        - system
      summary: Fixture health
      description: Returns the status.
      security: []
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Health"
        "500":
          $ref: "#/components/responses/InternalServerError"
components:
  schemas:
    Health:
      type: object
      description: Estado.
      additionalProperties: false
      required:
        - status
      properties:
        status:
          type: string
          description: Salud.
    ProblemDetails:
      type: object
      description: Problem Details (RFC 9457).
      additionalProperties: false
      required:
        - type
        - title
        - status
      properties:
        type:
          type: string
          description: Problem type.
        title:
          type: string
          description: Title.
        status:
          type: integer
          description: HTTP status.
  responses:
    InternalServerError:
      description: Error interno.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          example:
            type: urn:ope:problem:internal-error
            title: t
            status: 500
