# Fixture of tests/contract-rules: ope-collection-pagination.envelope. Generated by gen-fixtures.mjs from a minimal valid contract; violates only the rule in its name.
openapi: 3.1.0
info:
  title: Fixture
  version: 1.0.0
  description: Minimal test contract.
  contact:
    name: OPE
servers:
  - url: /
tags:
  - name: system
    description: Sistema.
  - name: portal
    description: portal.
paths:
  /v1/health:
    get:
      operationId: getHealth
      tags:
        - system
      summary: Fixture health
      description: Returns the status.
      security: []
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Health"
              example:
                status: ok
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/portal/decisions:
    get:
      operationId: listDecisions
      tags:
        - portal
      summary: Lists decisions
      description: Lists the decisions of the merchant.
      security:
        - portalSession: []
      x-required-capabilities:
        - ledger:read
      x-collection: true
      parameters:
        - $ref: "#/components/parameters/cursor"
        - $ref: "#/components/parameters/limit"
        - $ref: "#/components/parameters/from"
        - $ref: "#/components/parameters/to"
      responses:
        "200":
          description: Page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Health"
              example:
                items: []
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/portal/decisions/{decisionId}:
    get:
      operationId: getDecision
      tags:
        - portal
      summary: Reads a decision
      description: Reads one decision.
      security:
        - portalSession: []
      x-required-capabilities:
        - ledger:read
      parameters:
        - name: decisionId
          in: path
          required: true
          description: Decision.
          schema:
            type: string
      responses:
        "200":
          description: Decision.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Health"
              example:
                status: ok
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"
components:
  schemas:
    Health:
      type: object
      description: Estado.
      additionalProperties: false
      required:
        - status
      properties:
        status:
          type: string
          description: Salud.
    ProblemDetails:
      type: object
      description: Problem Details (RFC 9457).
      additionalProperties: false
      required:
        - type
        - title
        - status
      properties:
        type:
          type: string
          description: Problem type.
        title:
          type: string
          description: Title.
        status:
          type: integer
          description: HTTP status.
  responses:
    InternalServerError:
      description: Error interno.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          example:
            type: urn:ope:problem:internal-error
            title: t
            status: 500
    Unauthorized:
      description: No credential.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          example:
            type: urn:ope:problem:unauthorized
            title: t
            status: 401
  securitySchemes:
    portalSession:
      type: http
      scheme: bearer
      description: portalSession token.
  parameters:
    cursor:
      name: cursor
      in: query
      description: Cursor.
      schema:
        type: string
    limit:
      name: limit
      in: query
      description: Limit.
      schema:
        type: integer
        minimum: 1
        maximum: 100
    from:
      name: from
      in: query
      description: From.
      schema:
        type: string
        format: date-time
    to:
      name: to
      in: query
      description: To.
      schema:
        type: string
        format: date-time
