# Fixture of tests/contract-rules: ope-request-example. Generated by gen-fixtures.mjs from a minimal valid contract; violates only the rule in its name.
openapi: 3.1.0
info:
  title: Fixture
  version: 1.0.0
  description: Minimal test contract.
  contact:
    name: OPE
servers:
  - url: /
tags:
  - name: system
    description: Sistema.
paths:
  /v1/health:
    get:
      operationId: getHealth
      tags:
        - system
      summary: Fixture health
      description: Returns the status.
      security: []
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Health"
              example:
                status: ok
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/things:
    post:
      operationId: createThing
      tags:
        - system
      summary: Creates a thing
      description: Creates a thing.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ThingCreate"
      responses:
        "201":
          description: Creada.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Health"
              example:
                status: ok
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/ThingUnprocessable"
        "500":
          $ref: "#/components/responses/InternalServerError"
components:
  schemas:
    Health:
      type: object
      description: Estado.
      additionalProperties: false
      required:
        - status
      properties:
        status:
          type: string
          description: Salud.
    ProblemDetails:
      type: object
      description: Problem Details (RFC 9457).
      additionalProperties: false
      required:
        - type
        - title
        - status
      properties:
        type:
          type: string
          description: Problem type.
        title:
          type: string
          description: Title.
        status:
          type: integer
          description: HTTP status.
    ThingCreate:
      type: object
      description: Cosa a crear.
      additionalProperties: false
      required:
        - kind
      properties:
        kind:
          type: string
          description: Kind of thing.
          enum:
            - a
            - b
        meta:
          type: object
          description: Metadatos.
          additionalProperties: false
          properties:
            note:
              type: string
              description: Nota.
      x-invariants:
        - type: validation-failed
          status: 400
          rule: kind in [a, b]
          description: Valid kind.
  responses:
    InternalServerError:
      description: Error interno.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          example:
            type: urn:ope:problem:internal-error
            title: t
            status: 500
    BadRequest:
      description: Invalid request.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          example:
            type: urn:ope:problem:validation-failed
            title: t
            status: 400
    ThingUnprocessable:
      description: No procesable.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          example:
            type: urn:ope:problem:validation-failed
            title: t
            status: 422
