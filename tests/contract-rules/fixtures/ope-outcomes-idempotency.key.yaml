# Fixture of tests/contract-rules: ope-outcomes-idempotency.key. Generated by gen-fixtures.mjs from a minimal valid contract; violates only the rule in its name.
openapi: 3.1.0
info:
  title: Fixture
  version: 1.0.0
  description: Minimal test contract.
  contact:
    name: OPE
servers:
  - url: /
tags:
  - name: system
    description: Sistema.
  - name: outcomes
    description: outcomes.
paths:
  /v1/health:
    get:
      operationId: getHealth
      tags:
        - system
      summary: Fixture health
      description: Returns the status.
      security: []
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Health"
              example:
                status: ok
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/orders:
    post:
      operationId: notifyOrder
      tags:
        - outcomes
      summary: Notifies an order
      description: Notifies a confirmed order.
      security:
        - platformKey: []
      x-required-capabilities:
        - orders:write
      x-idempotency:
        key: amountless
        first: "201"
        repeat: "200"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OrderNotification"
            example:
              orderId: o-1
              amount: "10.00"
      responses:
        "200": &a1
          description: Recorded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Health"
              example:
                status: ok
        "201": *a1
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/OrderUnprocessable"
        "500":
          $ref: "#/components/responses/InternalServerError"
components:
  schemas:
    Health:
      type: object
      description: Estado.
      additionalProperties: false
      required:
        - status
      properties:
        status:
          type: string
          description: Salud.
    ProblemDetails:
      type: object
      description: Problem Details (RFC 9457).
      additionalProperties: false
      required:
        - type
        - title
        - status
      properties:
        type:
          type: string
          description: Problem type.
        title:
          type: string
          description: Title.
        status:
          type: integer
          description: HTTP status.
    OrderNotification:
      type: object
      description: Confirmed order.
      additionalProperties: false
      required:
        - orderId
        - amount
      properties:
        orderId:
          type: string
          description: Order identity of the platform.
        amount:
          type: string
          description: Amount.
      x-invariants:
        - type: validation-failed
          status: 400
          rule: amount is a decimal string
          description: Valid amount.
  responses:
    InternalServerError:
      description: Error interno.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          example:
            type: urn:ope:problem:internal-error
            title: t
            status: 500
    BadRequest:
      description: Invalid request.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          example:
            type: urn:ope:problem:validation-failed
            title: t
            status: 400
    Unauthorized:
      description: No credential.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          example:
            type: urn:ope:problem:unauthorized
            title: t
            status: 401
    Conflict:
      description: Conflict.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          example:
            type: urn:ope:problem:idempotency-conflict
            title: t
            status: 409
    OrderUnprocessable:
      description: Unprocessable.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          example:
            type: urn:ope:problem:validation-failed
            title: t
            status: 422
  securitySchemes:
    platformKey:
      type: apiKey
      in: header
      name: X-OPE-Platform-Key
      description: Platform key.
