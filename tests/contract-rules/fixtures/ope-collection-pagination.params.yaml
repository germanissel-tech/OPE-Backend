# Fixture of tests/contract-rules: ope-collection-pagination.params. Generated by gen-fixtures.mjs from a minimal valid contract; violates only the rule in its name.
openapi: 3.1.0
info:
  title: Fixture
  version: 1.0.0
  description: Minimal test contract.
  contact:
    name: OPE
servers:
  - url: /
tags:
  - name: system
    description: Sistema.
  - name: portal
    description: portal.
paths:
  /v1/health:
    get:
      operationId: getHealth
      tags:
        - system
      summary: Fixture health
      description: Returns the status.
      security: []
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Health"
              example:
                status: ok
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/portal/decisions:
    get:
      operationId: listDecisions
      tags:
        - portal
      summary: Lists decisions
      description: Lists the decisions of the merchant.
      security:
        - portalSession: []
      x-required-capabilities:
        - ledger:read
      x-collection: true
      parameters:
        - name: page
          in: query
          description: Page.
          schema:
            type: integer
        - name: offset
          in: query
          description: Offset.
          schema:
            type: integer
      responses:
        "200":
          description: Page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DecisionPage"
              example:
                items: []
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/portal/decisions/{decisionId}:
    get:
      operationId: getDecision
      tags:
        - portal
      summary: Reads a decision
      description: Reads one decision.
      security:
        - portalSession: []
      x-required-capabilities:
        - ledger:read
      parameters:
        - name: decisionId
          in: path
          required: true
          description: Decision.
          schema:
            type: string
      responses:
        "200":
          description: Decision.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Health"
              example:
                status: ok
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"
components:
  schemas:
    Health:
      type: object
      description: Estado.
      additionalProperties: false
      required:
        - status
      properties:
        status:
          type: string
          description: Salud.
    ProblemDetails:
      type: object
      description: Problem Details (RFC 9457).
      additionalProperties: false
      required:
        - type
        - title
        - status
      properties:
        type:
          type: string
          description: Problem type.
        title:
          type: string
          description: Title.
        status:
          type: integer
          description: HTTP status.
    DecisionPage:
      type: object
      description: Page of decisions.
      additionalProperties: false
      required:
        - items
      properties:
        items:
          type: array
          description: Decisions.
          items:
            $ref: "#/components/schemas/Health"
        nextCursor:
          type: string
          description: Next page.
  responses:
    InternalServerError:
      description: Error interno.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          example:
            type: urn:ope:problem:internal-error
            title: t
            status: 500
    Unauthorized:
      description: No credential.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetails"
          example:
            type: urn:ope:problem:unauthorized
            title: t
            status: 401
  securitySchemes:
    portalSession:
      type: http
      scheme: bearer
      description: portalSession token.
