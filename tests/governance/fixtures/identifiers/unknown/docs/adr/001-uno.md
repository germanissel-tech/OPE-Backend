---
numero: 1
estado: aceptada
---

# ADR-001

El caso de uso `notifyOrder` devuelve `merchantId` y `RETURNED`.

```ts
const enCodigo_no_se_juzga = "talle_calce";
```

Las barreras del contrato anterior eran `talle_calce` y `precio_valor`; el helper `sinNombre` tampoco existe.
