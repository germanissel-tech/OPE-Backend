description: El request cumple el contrato pero el manejador lo rechazó por una regla de dominio. `errors` enumera los motivos.
content:
  application/problem+json:
    schema:
      $ref: ../schemas/ProblemDetails.yaml
    example:
      type: urn:ope:problem:unprocessable
      title: Request válido rechazado por semántica
      status: 422
      instance: /v1/health
