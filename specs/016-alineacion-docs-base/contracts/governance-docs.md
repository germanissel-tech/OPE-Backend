# Documentos de gobernanza (R-01, R-02, R-03)

## Constitución 1.4.0

- Sync Impact Report (encabezado): 1.3.0 → 1.4.0; MINOR por XI; PATCH acumulado de barreras; nota de VII cerrada cuando `01 §10.3` diga siete campos (historia 3).
- `### XI. Ninguna política vive en el código` — texto íntegro de la evaluación §2.2, después de X.
- "Contrato de datos e identidad" → **Barreras del MVP**: exactamente tres — `fit` (talle y calce), `price` (precio y valor), `returns` (cambios y devoluciones).
- Pie: `**Version**: 1.4.0 | … | **Last Amended**: 2026-09-20`.

## ADR-025 (revisado)

Sección "Consecuencias" y la "Deuda declarada frente a la constitución X" reescritas: push es un modo; los tres modos; negociación por merchant; adaptadores en OPE; puerto único; el modo es configuración congelada; lo construido sigue vigente. Cita la evaluación y la feature 019 del mapa por nombre.

## ADR nuevo — Decisiones de producto confirmadas

Frontmatter (`numero`, `titulo`, `estado: aceptada`, `fecha: 2026-09-20`, `fuente: ../04-hoja-de-decisiones.md`); tabla D-B, D-C, D-E, D-F, D-G con propuesta, consecuencia en el repo (ADR-027 / spec 012; claims cerrados; riesgo por sesión; 021; 017) y estado "confirmada por el dueño el 2026-09-20".

## ADR-010 (corregido)

"Cada una se cierra en `01 §13` de los documentos del MVP"; párrafo nuevo: las decisiones de producto de `04` quedaron confirmadas (cita del ADR nuevo).

## Mapa del contrato

`features:` con 016–022 según R-03 y descripciones ampliadas; `operations.*.feature` renumeradas (016→017, 017→020, 018→021). Prosa del repo que cite una feature reservada por número: ninguna (015 la pasó a nombres); `check:api-map` verde.
