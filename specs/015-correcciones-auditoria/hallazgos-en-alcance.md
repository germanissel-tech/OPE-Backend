# Hallazgos en alcance de la 015

Extraídos de `docs/auditoria/trabajo/hallazgos/fase-{1..4}.json` (`status: confirmed`, 55); F-043, F-045, F-046 van a la feature de persistencia; F-041 se rechaza (R-13). La columna `historia` es la de `tasks.md`; el estado de cierre se escribe en la Phase 7.

| F-NNN | historia | severidad | archivo:línea                                                               | prueba propuesta                                                                                                                              |
| ----- | -------- | --------- | --------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| F-001 | US4      | low       | `src/domain/shared-kernel/index.ts:1`                                       | none: a header comment; the fix is the comment (scope: shared-kernel, axis 2)                                                                 |
| F-003 | US4      | low       | `src/application/shared-kernel/decorators/logged-use-case.ts:19`            | tests/unit/application/shared-kernel/logged-use-case.test.ts (existing cases keep passing; the change is the narrowing, not the behaviour)    |
| F-004 | US4      | low       | `src/composition/modules/shared-kernel.ts:14`                               | none: a rename; `tsc` and the profile test (tests/unit/composition/profile.test.ts) cover the wiring                                          |
| F-005 | US4      | low       | `docs/dominio/intervencion.md:13`                                           | none: a glossary note; `check:glossary` verifies existence and source, not currency                                                           |
| F-006 | US4      | low       | `tests/unit/health.test.ts:5`                                               | none: a move; `npm test` keeps running it                                                                                                     |
| F-007 | US2      | high      | `src/domain/merchant/merchant.ts:51`                                        | tests/unit/domain/merchant/merchant.test.ts: "[invariant] zero, more than two, or empty ingest keys reject the merchant; no origins reject i… |
| F-008 | US1      | medium    | `src/composition/modules/merchant.ts:3`                                     | none: a comment; ADR-018 retired the mock and the module list has no mock entry (tests/unit/main.test.ts: "nothing names a server mode, a mo… |
| F-009 | US4      | low       | `src/interface-adapters/http/security/ingest-key.ts:36`                     | tests/unit/http/security-handlers.test.ts: "finds the merchant whichever scheme resolved it" (moves with the function)                        |
| F-011 | US4      | medium    | `src/infrastructure/http/build-server.ts:110`                               | none: comments; `npm run lint` does not see a JSDoc attached to the wrong declaration                                                         |
| F-012 | US4      | low       | `src/infrastructure/http/build-server.ts:415`                               | tests/integration/server.test.ts and logging-privacy.test.ts (unchanged behaviour); handoff S-06                                              |
| F-013 | US3      | medium    | `src/infrastructure/http/cors.ts:30`                                        | tests/integration/cors.test.ts (Access-Control-Max-Age asserted); note: `no-magic-numbers` runs with `detectObjects: false`, so numbers in o… |
| F-014 | US1      | medium    | `CLAUDE.md:126`                                                             | none: prose; `check:adrs` verifies citations exist, not that they are current                                                                 |
| F-015 | US1      | medium    | `docs/adr/013-anillos-modulos-y-composicion.md:71`                          | none: prose (ADR-018 retired the modes)                                                                                                       |
| F-016 | US1      | medium    | `src/application/experiment/services/assignment.service.ts:3`               | none: a comment. `assignArm` was removed by feature 009 (specs/009-dominio-rico/tasks.md:80 "sin `assignArm`, `assignmentKey`, `fnv1a32`, `a… |
| F-017 | US1      | medium    | `src/composition/modules/experiment.ts:3`                                   | none: a comment. Feature 008 is `specs/008-casos-de-uso-y-errores/` and brings no store; contracts/api-map.yaml:206-222 assigns `createExper… |
| F-018 | US4      | low       | `tests/integration/assignment.test.ts:68`                                   | none today. The file imports `asExperimentId, asMerchantId, asVisitorId` at line 6 and still casts at 68, 120, 128, 137 and 139; `as never` … |
| F-019 | US4      | low       | `tests/unit/domain/experiment/assignment-regression.test.ts:15`             | none: a comment. The header (lines 1-4) explains the fingerprints, not why the hash is duplicated instead of imported; a reader who "dedupli… |
| F-020 | US4      | low       | `tests/unit/domain/experiment/assignment-regression.test.ts:1`              | none today. `FR-041` exists in specs 001, 002, 003, 005 and 009 with different meanings; the sibling tests/unit/domain/experiment/assignment… |
| F-021 | US3      | medium    | `src/interface-adapters/http/controllers/ingestion/ingest-events.ts:25`     | none today: `sonarjs/no-identical-functions` compares functions inside one file and `check:duplication` needs 50 tokens (this one has fewer)… |
| F-022 | US3      | low       | `src/domain/ingestion/errors.ts:19`                                         | tests/unit/domain/ingestion/event-batch.test.ts: after "[invariant:event-timestamp-out-of-range] timestamp out of tolerance → rejected", ass… |
| F-023 | US1      | medium    | `tests/unit/domain/ingestion/event-batch.test.ts:2`                         | none: a comment. The file has two `describe` blocks (`EventBatch.of`, `EventBatch.focus`) and no test about a NO_OP reason; the decision pla… |
| F-024 | US4      | low       | `tests/integration/ingest-events.test.ts:58`                                | the proposed assertion itself. spec 004 US2 scenario 3 (specs/004-protocolo-sdk-ingesta/spec.md:105) says "todos los eventos se reportan com… |
| F-025 | US4      | low       | `src/interface-adapters/gateways/ingestion/memory-event-dedup.ts:37`        | tests/unit/gateways/memory-event-dedup.test.ts pins the TTL and the cap ("time window: after 24 h the id comes in again", "size window: past… |
| F-026 | US4      | medium    | `src/domain/catalog/catalog-snapshot.ts:109`                                | none today: `check:dead-code` (knip) sees unused exports, not class methods, so `variant()` and the `#byVariant` map (built for the pilot si… |
| F-027 | US4      | low       | `src/application/catalog/services/product-truth.service.ts:16`              | none: a type. The only reader is src/application/decision/services/decision.service.ts:242 (`found.freshness.stockAndPrice`); nothing reads … |
| F-028 | US4      | low       | `src/domain/catalog/errors.ts:35`                                           | none: a name. The caller passes `snapshot.capturedAt` (src/application/catalog/use-cases/upsert-catalog-snapshot.use-case.ts:69) and the sna… |
| F-029 | US1      | low       | `src/application/catalog/policies/freshness.ts:3`                           | none: comments. Four comments in src/ cite a feature that is not built yet by its number — here and src/application/decision/ports/policy-di… |
| F-030 | US3      | medium    | `src/domain/barrier/barrier-rules.ts:52`                                    | none today: `sonarjs/no-identical-functions` compares within one file and `check:duplication` needs 50 tokens; one-line predicates never rea… |
| F-031 | US1      | medium    | `src/domain/commercial/commercial-policy.ts:38`                             | none today: a convention without a gate. CLAUDE.md:406 says "Porcentajes 0–100 sólo en el borde (DTO); adentro, tasas 0–1"; `src/domain/expe… |
| F-032 | US4      | low       | `src/domain/commercial/commercial-policy.ts:264`                            | none: a comment. `D-B` is a product decision of 03 §6 (specs/012-plano-de-decision-ii/spec.md:143 "cierra la decisión de producto D-B (03 §6… |
| F-033 | US3      | medium    | `src/interface-adapters/gateways/decision/memory-session-state-store.ts:22` | none today: `check:duplication` (jscpd) compares token sequences and the identifiers differ (`sessions`/`visitors`/`seen`, `maxSessions`/`ma… |
| F-034 | US1      | low       | `src/application/decision/policies/session-window.ts:2`                     | tests/unit/domain/decision/session-state.test.ts or a governance test: `expect(SESSION_WINDOW).toEqual({ ttlMs: DEDUP_WINDOW.ttlMs, maxSessi… |
| F-035 | US4      | low       | `src/application/decision/services/decision.service.ts:187`                 | none: a type. Both call-site arguments are `Barrier · undefined` (`settled.barrier`, `barrier` at line 133); `string` accepts any text and h… |
| F-036 | US1      | medium    | `src/application/ledger/ports/decision-id-generator.ts:2`                   | none: comments. Since 011 the only caller of `next()` is src/application/ledger/services/decision-recorder.service.ts:45,55 (`DefaultDecisio… |
| F-037 | US4      | low       | `src/application/outcomes/use-cases/notify-order.use-case.ts:86`            | tests/unit/domain/outcomes/order.test.ts would take the new method next to `withReturn`; tests/unit/application/outcomes/notify-order.test.t… |
| F-038 | US3      | medium    | `src/domain/selection/quality-gate.ts:73`                                   | none today: with a `default` branch `switch-exhaustiveness-check` is satisfied, and the template-literal member makes the union open, so the… |
| F-039 | US3      | low       | `src/composition/condition-config.ts:57`                                    | none today. A fact added to `FactCondition` (src/domain/barrier/condition.ts:35) is caught by the two exhaustive switches of the domain, but… |
| F-040 | US4      | low       | `src/application/outcomes/use-cases/notify-order.use-case.ts:54`            | none: a type. The use case calls `decisions.bySession` (line 82) and nothing else; `record` and `find` travel with it. ADR-028 decided the q… |
| F-041 | US4      | low       | `src/composition/modules/ingestion.ts:10`                                   | none: a design. Today `ingestionModule` compiles only against a `Ports` that has `sessions`, `visitors`, `policies`, `inference`, `catalog`,… |
| F-043 | persist  | high      | `src/application/ingestion/ports/event-dedup.ts:7`                          | tests/helpers/unavailable-ledgers.ts already fakes writes that answer `unavailable` (ADR-021 §6); the same helper with reads that answer `un… |
| F-044 | US2      | medium    | `src/application/catalog/ports/catalog-store.ts:9`                          | tests/unit/application/catalog/upsert-catalog-snapshot.use-case.test.ts with a store whose `replace` answers `unavailable` (tests/helpers/un… |
| F-045 | persist  | high      | `src/infrastructure/http/build-server.ts:181`                               | none today. A test that starts the server (tests/helpers/test-app.ts with a listening socket) and fires one `PUT /v1/catalog` of the pilot s… |
| F-046 | persist  | low       | `src/application/decision/services/decision.service.ts:89`                  | tests/integration/decision-plane.test.ts has "one intervention per session" with sequential batches; the interleaved case (two batches of th… |
| F-047 | US1      | low       | `src/interface-adapters/gateways/ledger/memory-decision-ledger.ts:9`        | none: a comment (or one paragraph in ADR-018 / profiles/local.ts). S-01 of the handoff, confirmed as stated: the five memory ledgers (`memor… |
| F-048 | US3      | low       | `src/application/merchant/policies/signature-window.ts:4`                   | tests/unit/domain/ingestion/event-batch.test.ts:71 ("the tolerance is the one the contract publishes") and its siblings pin each value separ… |
| F-051 | US2      | high      | `src/infrastructure/http/build-server.ts:183`                               | tests/integration/cors.test.ts: a preflight from a registered origin announcing `x-ope-platform-key` (and `x-ope-timestamp`/`x-ope-signature… |
| F-052 | US3      | medium    | `src/infrastructure/http/build-server.ts:121`                               | the full mutation report (reports/mutation/report.json; `trabajo/gates/mutation-full-summary.json`) is the proof: every `Ignored` mutant car… |
| F-053 | US2      | low       | `src/domain/merchant/merchant.ts:91`                                        | tests/unit/domain/merchant/merchant.test.ts: `ownsPlatformKey` with a key that shares a long prefix with a real one must take the same path … |
| F-054 | US3      | low       | `scripts/test-contract.mjs:53`                                              | `npm run test:contract` itself: today its output says "401 Unauthorized (3 operations): POST /v1/orders, POST /v1/returns, PUT /v1/catalog" … |
| F-055 | US3      | low       | `tests/audit/audit.test.ts:1`                                               | measurement of 2026-09-19 (vitest JSON reporter over the whole suite, 1004 tests, 147 s wall, 522 s of file time on the workers): tests/audi… |
| F-056 | US3      | low       | `tests/helpers/test-app.ts:142`                                             | none today. Every one of the 15 integration files that fixes the clock passes `occurredAt` explicitly (checked file by file), which is how t… |
| F-057 | US2      | low       | `src/infrastructure/http/build-server.ts:150`                               | tests/integration/ingest-events.test.ts: a `POST /v1/events` body of, say, 1 MiB must be refused with 413 before it is parsed (today it is b… |
| F-058 | US1      | low       | `docs/adr/029-firma-de-plataforma.md:26`                                    | tests/integration/platform-signature.test.ts: replaying the exact signed request (same timestamp, same body) inside the window must answer 2… |
| F-062 | US1      | high      | `contracts/components/schemas/Order.yaml:28`                                | none: a documentation decision. The field carries no buyer data (`{ kind: percent, value }`) and ADR-028 §5 decides the redemption is "un da… |
| F-063 | US1      | high      | `.specify/memory/constitution.md:179`                                       | none: a decision. What exists: three inbound operations with the platform credential (`contracts/api-map.yaml` consumer `platform`), no `Pla… |

## Rechazados

- F-041 (`src/composition/modules/ingestion.ts:10`): la propuesta rehace `wiring.ts` y los perfiles por un beneficio de legibilidad; la feature de persistencia toca los perfiles de todos modos (R-13).

## Hechos para el anexo de cierre

- F-024 (`tests/integration/ingest-events.test.ts`): la aserción propuesta (`bySession` con una sola decisión tras el reenvío) **falla**: el sistema decide cada lote y registra su decisión también cuando todos los eventos son duplicados; la deduplicación es de eventos, no de decisiones. La prueba pasa a afirmar lo que el sistema hace (dos decisiones con ids distintos, ningún evento registrado dos veces) y el hecho queda para la feature de persistencia, que decide si una decisión sobre un lote sin eventos nuevos debe registrarse.
- F-041: rechazado con motivo (arriba); riesgo anotado para la feature de persistencia, que va a tocar `wiring.ts` y los perfiles.
